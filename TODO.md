# Pending TODO

## Pakistani Serials

- [ ] Rename `monthlyViews` → `allTimeViews` everywhere (entity column, DB column `monthly_views` → `all_time_views`, view recording queries, v2-transform strip, Flutter model if exposed)
